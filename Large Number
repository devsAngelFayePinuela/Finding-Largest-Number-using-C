
#include <stdio.h>

int main() {
    int num1, num2, num3;
    int largest;
    
    printf ("Enter 3 values: \n");
    if (scanf("%d %d %d", &num1, &num2, &num3)!= 3){ //!=3 because you expect 3 values to input
    printf ("You enter invalid number.");
    
    return 0;
    
    }
    
    largest= num1; 
    
    if (largest < num2) {largest = num2;}
    if (largest < num3) {largest = num3;}
    
    
    printf ("The largest number is %d.", largest);
    
    return 0;
}
